{
  "name": "vue-mapp-kit",
  "description": "A wonderful collection of all things maps.",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^5.3.0"
  },
  "scripts": {
    "build": "npx lerna run build",
    "build:publish": "yarn build && npx lerna publish --dist-tag beta"
  }
}
